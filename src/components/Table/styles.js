import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    root: {
      width: '100%',
    },
    container: {
      maxHeight: 480,
      paddingLeft: 40,
      paddingRight: 40,
    },
  });
  